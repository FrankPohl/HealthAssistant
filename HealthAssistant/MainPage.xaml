<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HealthAssistant.MainPage"
             BackgroundColor="{DynamicResource SecondaryColor}">

    <ScrollView>
        <Grid RowSpacing="25" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,*"
              Padding="{OnPlatform iOS='30,60,30,30', Default='30'}" >
            <Button Grid.Row="0"
                Text="Init Deepgram"
                Clicked="OnInitDeepgramClicked"/>
            <Button Text="StartRecognition" Grid.Row="1" Clicked="OnStartAudioClicked"/>
            <Button Text="StopRecognition" Grid.Row="2" Clicked="OnStopAudioClicked"/>
            <Button Text="Restart" Grid.Row="3" Clicked="OnReOpenDeepgramConnectionClicked"/>
            <Label Text="Status" Grid.Row="4"/>
            <Label x:Name="Status" Grid.Row="5" MaxLines="20" />
        </Grid>
    </ScrollView>
</ContentPage>
